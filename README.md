# docker-alpine (x86_64 and armhf)
Alpine based images for Docker on regular Server (x86_64) and RaspberryPi (armhf)

```
git clone -b alpine https://github.com/christiansteier/dockerfiles.git --depth 1
```

