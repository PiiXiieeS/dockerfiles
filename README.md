# docker-debian (x86_64 and armhf)
Debian based images with S6 for Docker on regular Server (x86_64) and RaspberryPi (armhf)

```
git clone -b debian https://github.com/christiansteier/dockerfiles.git
```
